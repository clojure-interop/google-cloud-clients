(ns com.google.cloud.dns.DnsBatchResult
  "This class holds a single result of a batch call to the Cloud DNS."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.google.cloud.dns DnsBatchResult]))

