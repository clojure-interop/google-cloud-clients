(ns com.google.cloud.dns.spi.DnsRpcFactory
  "An interface for DnsRpc factory. Implementation will be loaded via ServiceLoader."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.google.cloud.dns.spi DnsRpcFactory]))

