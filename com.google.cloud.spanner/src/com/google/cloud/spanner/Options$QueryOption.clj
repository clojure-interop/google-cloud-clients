(ns com.google.cloud.spanner.Options$QueryOption
  "Marker interface to mark options applicable to query operation."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.google.cloud.spanner Options$QueryOption]))

